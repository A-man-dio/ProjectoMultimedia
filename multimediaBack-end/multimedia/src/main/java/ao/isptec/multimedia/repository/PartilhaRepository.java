package ao.isptec.multimedia.repository;

import ao.isptec.multimedia.model.Partilha;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PartilhaRepository extends JpaRepository<Partilha, Integer> {
}
