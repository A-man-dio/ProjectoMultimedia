package ao.isptec.multimedia.repository;

import ao.isptec.multimedia.model.MembroGrupo;
import ao.isptec.multimedia.model.MembroGrupoId;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MembroGrupoRepository extends JpaRepository<MembroGrupo, MembroGrupoId> {
}
